public class createAccount {
    public Account createAcc(String name)
    {
        Account acc23 = new Account();
        acc23.Name = name;
        return acc23;
        //system.debug('commiting from git to run job and check in target org2 created')
        //updating to check jobs on revision nums
        //checking incremental build fun'ty
    }

}